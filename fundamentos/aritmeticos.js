const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // sintax infix/binária

const subtracao = d - b

const multiplicacao = a * b

const divisao = d / a

const modulo = a % 2 // resto da divisão

console.log("Resultados:", '\n', "Soma:", soma, '\n', "Subtracao:", subtracao, '\n', "Multiplicação:", multiplicacao, '\n', "Divisão:", divisao, '\n', "Módulo:", modulo)
